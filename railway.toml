[build]
builder = "NIXPACKS"

[deploy]
startCommand = "cd /app/kon-mee-barn && npx prisma db push && npm start"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
